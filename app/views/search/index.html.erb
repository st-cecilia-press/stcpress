<h1>Search Results</h1>
<% @results.each do |piece| %>
  <h3><%= link_to piece["title"][0], piece["url"][0] %></h3>
  <% if piece["composer"] %>
  by: <%= piece["composer"][0] %>
    <% end %>
<% end %>
